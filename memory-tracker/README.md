# Memory Dependency Tracker

Visualizes file system dependencies as an interactive graph.

## Usage

```bash
npm install
npm run dev
```

## Features

- Interactive dependency graph using ReactFlow
- Visualizes file imports and relationships
- Drag nodes, zoom, and pan the graph
- Auto-layout based on dependency depth
